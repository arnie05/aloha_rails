
= form_for @fart do |f|
  -if @fart.errors.any?
    #error_explanation
      %h2= "#{pluralize(@fart.errors.count, "error")} prohibited this fart from being saved:"
      %ul
        - @fart.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :cool
    = f.text_area :cool
  .actions
    = f.submit 'Save'
